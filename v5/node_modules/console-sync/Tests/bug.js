require("../index");

const testf = require("./testf").testf;

console.dir(console);

var a;
for(var i=0; ;++i)
{
   console.log("Value of i=%d",i);
   console.info("info i=%d",i);
   console.error("invalid Value of i=%d",i);
   console.warn("warn Value of i=%d",i);
   testf(i*2);
   console.assert(false,"invalid Value of i=%d",i);

   if ( i && i%1000000 === 0)
   {
      a = new Array(10000000);
   }
   else if ( i && i%2000000 === 0)
   {
      a = null;
   }
}